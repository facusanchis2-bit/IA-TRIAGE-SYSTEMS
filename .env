# Credenciales panel
ADMIN_USER=admin
ADMIN_PASS=admin

# Sal para hash de paciente (cambiala en prod)
PHI_SALT=cambiame-por-una-sal-larga

# Base de datos (Postgres local por Docker)
DATABASE_URL=postgresql+psycopg://triage:triage@localhost:5432/triage
# --- DB pool & timeouts ---
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=5
DB_POOL_RECYCLE=1800
DB_POOL_TIMEOUT=10
# --- DB pool & timeouts ---
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=5
DB_POOL_RECYCLE=1800
DB_POOL_TIMEOUT=10
VITE_API_URL=http://localhost:8000
